
# The hostname should be specified at buildtime. 
# I need to read the correct hostname from the config files.

set test_name "hostname"

send "hostname\r" 

expect {
    ITMDKLT097 {
        pass $test_name 
    }
    "#" {
        fail "$test_name returned to prompt. $expect_out(buffer)" 
    }
    timeout {
        fail "$test_name timed out." 
    }
}
